<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appflorentinobeautieofficial">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppFlorentinoBeautieOfficial">
        <activity android:name=".Detail_RatingBar"></activity>
        <activity android:name=".HairCareActivity" />
        <activity android:name=".SkinCareActivity" />
        <activity android:name=".OthersActivity" />
        <activity android:name=".MakeUpActivity" />
        <activity android:name=".NewProductActivity" />
        <activity android:name=".FlashSaleActivity" />
        <activity android:name=".ProductDetailsActivity" />
        <activity android:name=".Home" />
        <activity android:name=".IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>